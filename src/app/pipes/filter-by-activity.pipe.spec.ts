import { FilterByActivityPipe } from './filter-by-activity.pipe';

describe('FilterByActivityPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByActivityPipe();
    expect(pipe).toBeTruthy();
  });
});
